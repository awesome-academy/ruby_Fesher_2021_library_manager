$('#comment_form').html('<%= j render partial: "shared/comment_form", locals: {object: @commentable} %>');
$('#comment-section').html('<%= j render partial: "shared/comment", collection: @comments %>');
$('#comment_count').html('<%= @comments.count %>');
<% unless @comment_build.valid? %>
  toastr.error('<%= j t ".fail" %>')
<% end %>
